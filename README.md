# sugarsaltdesign — Next.js site

## Quick start
```bash
npm i
npm run dev
```

## Environment
Copy `.env.example` to `.env.local` and fill in values.

## Deploy
Vercel recommended. Add env vars in the dashboard.

## Notion CMS
Replace demo data by wiring to Notion using `NOTION_WORK_DB_ID` and `NOTION_JOURNAL_DB_ID`.
